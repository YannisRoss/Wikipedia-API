wikientries index

<% if current_user%>
        <% unless WikiEntry.all.empty?%>
            <ul>
            <li>Entries count: <%= WikiEntry.all.size%> </li>
            <li>Search terms: <%= SearchTerm.all.size%> </li>   

            <li>Entry with the biggest wordcount: <%= WikiEntry.all.order(:wordcount).limit(1).title%></li>
            
            <li>Entry with the lowest wordcount: <%= WikiEntry.all.order(wordcount: :desc).limit(1).title%></li>

            
            - Entry with the most words in title
            - Entry with the lowest levenshtein distance according to its search term (levenshtein value and search term)
            - Top 5 favourited entries

            </ul>
        <% else %>
            <p> No entries yet. </p>
        <% end %>

<% end %>